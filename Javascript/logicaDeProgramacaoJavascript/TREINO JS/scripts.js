// Object

const person = {
    name: 'john',
    age: 30,
    weight: 88.6,
    isAdmin: true 
}

console.log(person)

// Para puxar o valor de um nome ou de outras variavel
console.log(person.name)

// Para escrever os dados inteiros
console.log(`${person.name} tem ${person.age}`)